export type Character = {
	name: string,
    description: string,
	projects: string[],
    users: string[]
}

export type CharacterWithId = {
    id: string,
	name: string,
    description: string,
	projects: string[],
    users: string[]
}